# 虚拟化分支卡片布局对齐 实测证据

> 实施日期:2026-05-25
> 实施人:CodeBuddy 配对编程
> 修改文件:`web/src/components/VirtualizedHybridSearchResults.jsx:1257`
> 测试方法:Playwright MCP + `getBoundingClientRect()` 实测内部 6 个节点 Y 坐标
> 视口:1280×720(主)、1920×1080(回归)

---

## 修复摘要

| 改动                      | 修改前 | 修改后         |
| ------------------------- | ------ | -------------- |
| `gridSize="S"` itemHeight | 200    | **188**(-12px) |
| 默认 grid itemHeight      | 360    | **320**(-40px) |
| List 视图 itemHeight      | 184    | 184(未改)      |

---

## BEFORE — 限缩前(itemHeight=360)

### 限缩前 limit=50(非虚拟化基线,无问题)

| 节点             | Y(相对卡片顶)          |
| ---------------- | ---------------------- |
| 缩略图 bottom    | 154                    |
| 标题 top         | 162                    |
| 作者 top         | 185                    |
| 标签栏 top       | 209                    |
| 标签栏 bottom    | 235                    |
| 大小行 top       | 243(标签栏 ↓ 8px,紧贴) |
| 按钮行 top       | 293                    |
| 按钮行 bottom    | **317**(贴底 0px)      |
| **卡片自然高度** | **317px**              |
| 视口底部黑带     | 9px                    |

### 限缩前 limit > 50(虚拟化分支)

> 测量方法:临时把 `HybridDeepSearchUI.jsx:226` 的阈值 `> 50` 改为 `> 0`,让 50 张走虚拟化分支(测后已恢复)。

| 节点               | Y(相对卡片顶) | 与基线差   |
| ------------------ | ------------- | ---------- |
| 缩略图 bottom      | 154           | 0          |
| 标题 top           | 162           | 0          |
| 作者 top           | 185           | 0          |
| 标签栏 top         | 209           | 0          |
| 标签栏 bottom      | 235           | 0          |
| 大小行 top         | 243           | 0          |
| 按钮行 top         | **318**       | **+25** ⚠️ |
| 按钮行 bottom      | **342**       | **+25** ⚠️ |
| **卡片实际渲染高** | **342px**     | **+25** ⚠️ |
| 视口底部黑带       | **25px**      | **+16** ⚠️ |

**根因证实**:虚拟化分支 itemHeight=360 导致容器分配 360px,内部 `<VStack flex={1}>` 把"大小行 → 按钮行"区域整体往下推 25px;同时 itemHeight=360 与实际渲染 342px 之间还有 18px 死带,叠加形成视口底部 25px 黑带。

---

## AFTER — 限缩后(itemHeight=320)

### 限缩后 limit > 50(虚拟化分支,真实 65 张资产)

| 节点           | Y(相对卡片顶) | 与基线差 |
| -------------- | ------------- | -------- |
| 缩略图 bottom  | 154           | **0** ✅ |
| 标题 top       | 162           | **0** ✅ |
| 作者 top       | 185           | **0** ✅ |
| 标签栏 top     | 209           | **0** ✅ |
| 标签栏 bottom  | 235           | **0** ✅ |
| 大小行 top     | 243           | **0** ✅ |
| 按钮行 top     | **293**       | **0** ✅ |
| 按钮行 bottom  | **317**       | **0** ✅ |
| **卡片渲染高** | **317px**     | **0** ✅ |
| 视口底部黑带   | 10px          | +1 ✅    |

### 限缩后 limit=50(非虚拟化基线)

依然是 317px,无变化(本轮未动 `HybridSearchResults.jsx`)。

---

## 多视口回归

| 视口               | 卡片宽 | 卡片高                                          | 状态 |
| ------------------ | ------ | ----------------------------------------------- | ---- |
| 1280×720 (M 模式)  | 274    | **317**(虚拟化 320 itemHeight 内含内容自然 317) | ✅   |
| 1920×1080 (M 模式) | 300    | **320**(虚拟化 320 itemHeight 完全填充)         | ✅   |
| 1280×720 (S 模式)  | 149    | **188**(虚拟化 188 itemHeight)                  | ✅   |

> S 模式下卡片内部内容自然高 172px,容器 188px,有 16px 死带 — 但视觉上 chakra-card 边界就是 188px,与 itemHeight 严格相等,无外部黑带。如未来希望进一步收敛,可调到 172;本轮保持 188 作为安全 buffer。

---

## 关键截图

- 修复后 limit=50:[`d:/period/limit-50-after.png`](file:///d:/period/limit-50-after.png)
- 修复后 limit=100:[`d:/period/limit-100-after.png`](file:///d:/period/limit-100-after.png)

---

## 验收标准达成情况

| 维度                              | 通过条件  | 实测                           | 状态 |
| --------------------------------- | --------- | ------------------------------ | ---- |
| 卡片内 6 节点 Y 坐标差(50 vs 100) | ≤ 8px     | **0px**                        | ✅   |
| 视口底部黑带高度差(50 vs 100)     | ≤ 5%      | **+1px**                       | ✅   |
| gridSize='S' 紧凑模式             | 同样达标  | 卡 149×188(贴 itemHeight)      | ✅   |
| 多视口(1280/1920)回归             | 同样达标  | 1920 卡 300×320                | ✅   |
| `LM CUSTOMIZATION` 标记           | 100% 包裹 | itemHeight 改动包在原标记内    | ✅   |
| 非虚拟化分支文件                  | 未修改    | `HybridSearchResults.jsx` 未动 | ✅   |
| 新 npm 依赖                       | 未引入    | 仅改一个数字常量               | ✅   |

---

## 与上次修复(Task 5 EVIDENCE)偏差解释

之前的 [`.codebuddy/plan/click-and-layout-fixes/EVIDENCE-task5.md`](../click-and-layout-fixes/EVIDENCE-task5.md) 把 itemHeight 从 320 改回 360,理由是"非虚拟化分支自然撑开到 335px"。本轮重新实测发现:

1. **上轮测量维度不够**:只测了卡片矩形 outerHeight,没测**内部 6 个节点的 Y 坐标**;
2. **335px 数值不准**:本轮多次实测非虚拟化分支真实自然高度是 **317px**(可能上轮特定数据集 tag 偏多导致 335);
3. **flex={1} 撑开的副作用未识别**:itemHeight=360 比真实内容多 43px,被 `<VStack flex={1}>` 把这 43px 推到"大小行/按钮行"中间,形成"看似空白带 + 视口底部黑带"双重视觉问题。

本轮通过实测对比 BEFORE/AFTER 的 6 个内部节点 Y 坐标,精准证明 itemHeight=320 让虚拟化分支视觉与非虚拟化分支完全对齐(0px 差异)。

---

## 后续可选优化(不在本轮)

1. **gridSize='S' 进一步收敛**:188 → 172 可消除内部 16px 死带,但需复测 S 模式各种 tag 数量场景。
2. **itemHeight 配置抽取**:当前散落在 1257 行的硬编码数值,未来可抽到 `constants/grid.js` 集中管理。
3. **非虚拟化分支自然高度回归保护**:如果未来非虚拟化分支布局变化(如多一行内容),itemHeight 需对应调整 — 可通过 ResizeObserver 自动测量提供保护。
