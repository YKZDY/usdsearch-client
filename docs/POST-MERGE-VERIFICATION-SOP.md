# 合并验证 SOP：integration/lm-merge-acd → lm

> **日期**：2026-05-26  
> **合并提交**：`4680190`  
> **备份 tag**：`pre-merge-acd-backup`（回滚命令：`git reset --hard pre-merge-acd-backup`）  
> **编译状态**：✅ 已通过

---

## 前置准备

```bash
cd d:/period/usdsearch-client/web
npm start
# 等待 dev server 启动后，打开 http://localhost:3000
```

---

## 阶段一：冒烟验证（5 分钟）

快速走通核心路径，确认没有白屏/崩溃：

| # | 操作 | 预期结果 | ✅/❌ |
|---|------|----------|-------|
| 1 | 打开 http://localhost:3000 | 页面加载，无白屏，无控制台红色报错 | |
| 2 | 若出现登录界面 → 点"使用离岸太湖 SSO 登录" | 弹窗打开，登录完成后弹窗关闭 | |
| 3 | 在搜索框输入关键词 → 按 Enter | 搜索结果卡片正常渲染（有缩略图） | |
| 4 | 单击任一卡片 | 右侧 Drawer 滑出显示详情 | |
| 5 | 滚动到底 | 自动加载更多（"Loading more..." 出现后消失） | |

---

## 阶段二：SSO 登录与 Auth 模块（高优先级）

> **风险点**：lm 上 SSO 是独有功能，integration 未触及但合入了大量 UI 改动，需确认不干扰。

| # | 测试项 | 操作步骤 | 预期结果 | ✅/❌ |
|---|--------|----------|----------|-------|
| S1 | SSO 弹窗打开 | 清除 localStorage → 刷新 → 点"使用离岸太湖 SSO 登录" | 新弹窗/新标签打开 IOA 登录页 | |
| S2 | SSO Token 回传 | 在 IOA 页面完成登录 | 弹窗自动关闭，主页刷新为已登录状态 | |
| S3 | 弹窗被拦截提示 | 浏览器设置阻止弹窗 → 点击登录 | 显示"登录弹窗被浏览器拦截"提示 | |
| S4 | Token 过期重连 | 登录后在 DevTools Application → Local Storage 手动删除 token → 等待下次 WSS 心跳 | 出现"重新登录"引导，不白屏 | |
| S5 | Auth 弹窗 z-index | 已打开 Drawer 状态下 → 手动触发 auth 过期 | Auth 重登录弹窗在 Drawer 之上显示，可操作 | |

---

## 阶段三：选择交互（Group A - SINGLE_CLICK_DRAWER 模式）

> **风险点**：`useDrawerOrSelect` + `useClickOrDoubleClick` 全新逻辑

| # | 测试项 | 操作步骤 | 预期结果 | ✅/❌ |
|---|--------|----------|----------|-------|
| A1 | 单击开 Drawer | 单击卡片本体（非复选框） | 右侧 Drawer 滑出，不进入多选 | |
| A2 | 复选框 toggle | 点击卡片左上角复选框 | 该卡片被选中（有视觉反馈），不开 Drawer | |
| A3 | Ctrl+Click | 按住 Ctrl 单击另一张卡片 | toggle 该卡片选中，不开 Drawer | |
| A4 | Shift+复选框 | 先点卡片 A 复选框 → Shift+点卡片 D 复选框 | A~D 区间全部被选中 | |
| A5 | Shift+卡片本体 | 先点卡片 A 复选框 → Shift+点卡片 D 本体 | 仅 A~D 区间选中（覆盖式） | |
| A6 | ESC 退出多选 | 选中多张后按 ESC | 清除所有选中，退出多选模式 | |
| A7 | Drawer 切换 | 开着 Drawer → 单击另一张卡片 | Drawer 内容切换为新卡片（不关再开） | |

---

## 阶段四：拖拽框选与多选（useDragSelect）

> **风险点**：`useDragSelect.js` 大改 +253 行，polyfill + autoScroll

| # | 测试项 | 操作步骤 | 预期结果 | ✅/❌ |
|---|--------|----------|----------|-------|
| D1 | 基本框选 | 在空白区域长按拖拽，经过多张卡片 | 出现蓝色半透框，经过的卡片被选中 | |
| D2 | autoScroll | 拖拽到容器边缘不松手 | 列表自动滚动，新滚出的卡片自动加入选区 | |
| D3 | 框选+取消 | 框选 5 张 → 再次从空白拖拽（无修饰键） | 之前的选中被清除，只选新框区域 | |
| D4 | 框选+追加 | 框选 3 张 → Ctrl+从空白拖拽 | 追加到现有选区 | |
| D5 | 无 TDZ 报错 | 打开 DevTools Console → 执行任意框选 | 控制台无 "Cannot access before initialization" | |
| D6 | 拖拽不卡 | 快速拖拽框选 20+ 张 | 流畅无明显卡顿（检查 DevTools Performance 无 >50ms 长任务） | |

---

## 阶段五：虚拟化列表与无限滚动

> **风险点**：`VirtualizedHybridSearchResults.jsx` +393 行

| # | 测试项 | 操作步骤 | 预期结果 | ✅/❌ |
|---|--------|----------|----------|-------|
| V1 | 虚拟化渲染 | 搜索返回 >50 条 → 检查 DOM 节点数 | DOM 中实际渲染的卡片数远少于总数（约 20-30 个） | |
| V2 | 滚动到底加载 | 滚动到底 | 出现"Loading more..."→ 加载完成后新卡片出现 | |
| V3 | 无底部黑带 | 滚动到任意位置停下 | 卡片底部无 49px 黑色空隙 | |
| V4 | 卡片高度一致 | 不同列卡片 | 所有卡片高度对齐，无挤压 | |
| V5 | 快速滚动稳定 | 快速滚动列表 | 无白屏闪烁，无跳跃 | |
| V6 | 横向无抖动 | 滚动时观察页面 | 无水平方向的左右抖动（scrollbar-gutter 生效） | |

---

## 阶段六：搜索设置（Group C - SearchSettingsPopover）

> **风险点**：新组件，依赖 `searchWeightMap.js` + `config.jsx`

| # | 测试项 | 操作步骤 | 预期结果 | ✅/❌ |
|---|--------|----------|----------|-------|
| C1 | Popover 打开 | 点击搜索栏旁边的设置齿轮图标 | 弹出 SearchSettingsPopover | |
| C2 | Tag 权重滑块 | 拖动 Tag Weight Slider | 滑块值变化（5 个预设 + 自定义） | |
| C3 | 重置功能 | 改动设置 → 点击 Reset | 所有设置恢复默认值 | |
| C4 | 设置持久化 | 改动设置 → 刷新页面 | 设置保留（localStorage） | |
| C5 | 设置影响搜索 | 调整权重后重新搜索 | 搜索结果排序有变化 | |

---

## 阶段七：卡片 Tag 栏（Group B - CardTagBar）

> **风险点**：新组件，依赖 `useAssetTags` + `taggingService.js`

| # | 测试项 | 操作步骤 | 预期结果 | ✅/❌ |
|---|--------|----------|----------|-------|
| T1 | Tag 显示 | 搜索有 tag 的资产 | 卡片底部显示 tag pills | |
| T2 | Tag 栏不溢出 | 查看有 4+ tag 的卡片 | 单行显示 + "+N more" 折叠 | |
| T3 | 添加 Tag | 点击卡片 tag 栏"+"按钮 → 输入新 tag → Enter | Tag 出现在卡片上 | |
| T4 | 删除 Tag | 点击已有 tag 的"×" | 出现 5 秒撤销 toast → 5 秒后真正删除 | |
| T5 | 撤销删除 | 删除 tag 后在 5 秒内点"撤销" / 按 Ctrl+Z | Tag 恢复 | |
| T6 | 未登录状态 | 退出登录 → 查看 tag 栏 | 显示"登录后可管理标签"，添加/删除不可用 | |

---

## 阶段八：Drawer 详情面板（AssetDetailsDrawer）

> **风险点**：849 行新组件 + drawer-panels 子模块

| # | 测试项 | 操作步骤 | 预期结果 | ✅/❌ |
|---|--------|----------|----------|-------|
| W1 | Drawer 基本渲染 | 单击卡片 | Drawer 从右滑入，显示大图+基本信息 | |
| W2 | Drawer 关闭 | 点 Drawer 外部区域 / 按 ESC | Drawer 关闭动画正常 | |
| W3 | Tags 面板 | 在 Drawer 中查看/编辑 tags | AssetTagEditor 正常工作 | |
| W4 | Advanced 面板 | 展开高级面板 | 显示匹配字段、分数、向量相似度等 | |
| W5 | 快速切换 | 连续点击不同卡片（每秒 2-3 次） | Drawer 内容即时更新，无残留旧数据 | |
| W6 | 关闭后立即点击 | 关闭 Drawer（动画中）→ 立即点另一张卡片 | 新 Drawer 正常打开（无"点击无反应"） | |
| W7 | Drawer + FAB 不遮挡 | 开着 Drawer → 查看 FAB 工具栏位置 | FAB 按钮不被 Drawer 遮挡 | |

---

## 阶段九：国际化（i18n）

| # | 测试项 | 操作步骤 | 预期结果 | ✅/❌ |
|---|--------|----------|----------|-------|
| I1 | 中文模式 | 切换到中文 | 所有新功能文案显示中文（无 key 裸露） | |
| I2 | 英文模式 | 切换到英文 | 所有新功能文案显示英文 | |
| I3 | SSO 文案 | 中文模式下查看 SSO 登录 | 显示"使用离岸太湖 SSO 登录" | |

---

## 回滚方案

如果验证中发现严重问题：

```bash
cd d:/period/usdsearch-client
git reset --hard pre-merge-acd-backup
# 回到合并前的状态
```

---

## 验证完成检查清单

- [ ] 阶段一全部 ✅
- [ ] 阶段二 S1-S5 全部 ✅
- [ ] 阶段三 A1-A7 全部 ✅
- [ ] 阶段四 D1-D6 全部 ✅
- [ ] 阶段五 V1-V6 全部 ✅
- [ ] 阶段六 C1-C5 全部 ✅
- [ ] 阶段七 T1-T6 全部 ✅
- [ ] 阶段八 W1-W7 全部 ✅
- [ ] 阶段九 I1-I3 全部 ✅

**全部通过 → 可以 `git push origin lm` 推送到远端。**
