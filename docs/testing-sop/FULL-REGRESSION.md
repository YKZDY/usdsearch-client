# 完整回归测试 (FULL-REGRESSION)

> **用途**：发版前 / 定期巡检使用。覆盖全部 [P0] + [P1] + 状态交叉联动 + 历史翻车回归。
> **预计耗时**：~2 小时
> **环境**：部署环境 + 隐私模式浏览器

---

## M1 搜索核心

### [P0] 核心路径

- [ ] 输入关键词 → 回车/点击搜索 → 返回结果
- [ ] 搜索空关键词：有默认行为或提示
- [ ] 分页：首页 → 下一页 → 上一页均正常
- [ ] limit 参数生效（默认 50 条，改为 10/100 后结果数正确）
- [ ] URL 参数同步：搜索后 URL 包含 query，刷新页面恢复搜索状态
- [ ] 以图搜图：拖拽图片到搜索框 → 返回相似结果
- [ ] 以图搜图：点击相机图标上传 → 返回结果
- [ ] 清除图片：点击预览 X → 恢复纯文本搜索

### [P1] 边界 & 异常

- [ ] 搜索结果 total 数字显示正确
- [ ] 快速连续搜索（输入A→立即输入B）：只显示最后一次的结果
- [ ] 网络超时/后端报错时的错误提示
- [ ] 搜索词含特殊字符（中文、空格、引号、&）正常工作
- [ ] embedding_knn_search_method 切换（exact vs approximate）
- [ ] 图片+文本组合搜索
- [ ] 搜索历史：搜索后聚焦输入框 → 下拉出现历史记录
- [ ] 搜索历史：点击历史项 → 自动填入并搜索
- [ ] 搜索历史：删除单条 / 清除全部历史
- [ ] 搜索历史上限（超过 10 条删除最早的）

---

## M2 搜索过滤器

### [P0] 核心路径

- [ ] 文件扩展名过滤（include/exclude）生效
- [ ] 路径过滤：输入 search_path → 结果均在该路径下
- [ ] 日期过滤：created_after/before 范围正确
- [ ] 尺寸过滤：file_size_greater_than/less_than 生效
- [ ] 标签过滤：选中标签 → 结果仅含该标签
- [ ] 多个过滤器组合使用时结果正确（AND 逻辑）
- [ ] 清除过滤器 → 恢复全量结果
- [ ] 分类侧边栏选中分类 → 发送 filter_by_tags
- [ ] 选"所有产品" → 清除分类过滤
- [ ] 二级分类点击生效

### [P1] 边界 & 异常

- [ ] 维度过滤（bbox_x/y/z）生效
- [ ] 用户过滤（created_by/modified_by）
- [ ] filter_url_regexp 正则匹配
- [ ] deduplicate_by_hash 开启后去重
- [ ] similarity_threshold / cutoff_threshold 边界值（0、1、超出范围）
- [ ] 过滤器记忆：刷新页面后过滤器状态保持
- [ ] MemoryChip 显示当前活跃过滤器数量
- [ ] 自定义搜索参数记忆：输入 500-2000 → 快捷选择列表新增此项
- [ ] 快捷选择列表上限（最多 10 个，超出删除最早的）
- [ ] 分类 URL 参数 ?category=xxx 刷新后保持
- [ ] 浏览器前进/后退 → 分类状态跟随

---

## M3 搜索结果展示

### [P0] 核心路径

- [ ] Grid 视图正常展示缩略图网格
- [ ] List 视图正常展示列表
- [ ] Grid 大小切换（L/S）布局正确
- [ ] "仅显示有预览图的结果" 开关生效
- [ ] 点击结果卡片 → 打开资产详情模态框
- [ ] 结果数量显示与实际一致
- [ ] 卡片 hover 效果正常

### [P1] 边界 & 异常

- [ ] 虚拟滚动：快速滚动不白屏、不丢失项
- [ ] 0 条结果时的空状态提示（EmptySearchHint）
- [ ] 分数显示开关：开启后每个卡片显示 relevance score
- [ ] 长文件名截断显示 + tooltip 完整路径
- [ ] 搜索结果加载中 skeleton/spinner 展示
- [ ] 搜索结果中标签 badge 正确显示

---

## M4 图片加载系统

### [P0] 核心路径

- [ ] 搜索结果缩略图正常加载
- [ ] 三级缓存生效：第二次加载同图更快
- [ ] 无缩略图的资产显示 placeholder

### [P1] 边界 & 异常

- [ ] 清除 IndexedDB 后图片从 API 重新加载
- [ ] 大量结果（>50）快速滚动图片懒加载正常
- [ ] 图片加载失败显示 FailedBadge
- [ ] 长时间未操作后缓存过期图片自动刷新

---

## M5 资产详情

### [P0] 核心路径

- [ ] 模态框打开，显示资产名称/路径/属性
- [ ] 缩略图正常展示
- [ ] 标签列表正确
- [ ] 添加标签 → 立即出现
- [ ] 删除标签 → 立即移除
- [ ] 关闭模态框 → 卡片标签同步更新
- [ ] Reindex 按钮触发成功

### [P1] 边界 & 异常

- [ ] 标签输入自动补全（从 globalTags）
- [ ] 添加重复标签的处理
- [ ] 标签名含特殊字符（空格、中文、符号）
- [ ] 网络断开时增删标签的错误提示
- [ ] 资产属性展示完整（dimensions, file_size, created_by）
- [ ] USD 属性展示
- [ ] 依赖关系（dependencies）展示
- [ ] 复制资产路径功能正常

---

## M6 标签系统

### [P0] 核心路径

- [ ] globalTags 加载完成
- [ ] TagsFilter 选中标签 → 过滤正确
- [ ] 详情弹窗增删标签 → globalTags 缓存同步
- [ ] FabToolbar 标签筛选联动
- [ ] 点击候选标签 → 添加到当前资产（非整页）
- [ ] 部署环境 tag 增删正常
- [ ] 新增标签后搜索 → 该标签出现在结果 tag 字段

### [P1] 边界 & 异常

- [ ] globalTags 为空时的空状态
- [ ] 大量标签时性能不卡顿
- [ ] 批量打标签功能（BATCH_TAGGING flag 开启后）
- [ ] 标签去重：搜索词已含标签名不重复加权
- [ ] TaggedBadge 即时更新
- [ ] 默认展开标签 section

---

## M7 目录树

### [P0] 核心路径

- [ ] 登录后目录树正常加载
- [ ] 点击节点 → search_path 填入 → 搜索触发
- [ ] 搜索结果动态注入路径节点
- [ ] 未登录显示静态骨架 fallback

### [P1] 边界 & 异常

- [ ] 展开/折叠交互正常
- [ ] 大目录（>100 节点）不卡顿
- [ ] 加载失败时错误 banner 提示
- [ ] 节点显示资产计数正确
- [ ] PathTreeBrowser 搜索/过滤功能

---

## M8 认证 & 服务器

### [P0] 核心路径

- [ ] Basic Auth 登录成功 → 可正常搜索
- [ ] 登出 → 未认证状态
- [ ] 认证持久化：刷新后保持登录
- [ ] ?server=nucleus 参数生效
- [ ] 未认证搜索可用（公共数据）
- [ ] Token 过期 → 弹窗引导重新登录
- [ ] 未登录搜索 → 弹出登录引导
- [ ] 页面加载自动检测后端
- [ ] "清除令牌" → 自动跳转登录界面

### [P1] 边界 & 异常

- [ ] Device Flow 认证流程（ENABLE_NUCLEUS_AUTH=true）
- [ ] API Key 认证方式
- [ ] 多服务器切换：nucleus → 其他 → 回来，状态不混乱
- [ ] 认证过期自动重新认证
- [ ] x-usdsearch-storage-backend header 正确携带
- [ ] URL 别名兼容：?server=omniverse 等同 ?server=nucleus
- [ ] 后端不可达时的错误处理（不白屏、有提示）

---

## M9 多选 & 交互

### [P0] 核心路径

- [ ] 单击卡片 = 查看详情
- [ ] 双击卡片 = 查看详情（兼容）
- [ ] Hover 左上角 → checkbox 出现
- [ ] 点击 checkbox → 选中（不触发详情）
- [ ] 选中 ≥1 → 所有卡片点击变为选中模式
- [ ] 选中状态下双击仍可打开详情
- [ ] 取消全部选中 → 恢复单击=详情
- [ ] SelectionModeBar 无页面抖动
- [ ] SelectionModeBar 覆盖筛选栏（不造成卡片位移）
- [ ] 多选状态下禁止切换筛选器

### [P1] 边界 & 异常

- [ ] 拖拽多选：空白处拖拽 → 矩形框选（模仿 Windows 文件管理器）
- [ ] 卡片内拖拽 → 选择文本（不触发框选）
- [ ] Shift+点击 = 范围合并多选
- [ ] 无 Shift 时点击 = 重新选择
- [ ] 选中动画无明显延迟
- [ ] checkbox 与 TaggedBadge 不重叠
- [ ] 卡片描边（选中态）不出界
- [ ] 选中状态翻页后清除
- [ ] 重新搜索后选中状态清除
- [ ] 键盘快捷键（Ctrl+A 全选等）

---

## M11 i18n & 视觉一致性

### [P0] 核心路径

- [ ] 中文模式所有可见文案为中文
- [ ] toast 提示信息中文
- [ ] 弹窗内文案完整中文化

### [P1] 边界 & 异常

- [ ] 长中文文案无截断/溢出
- [ ] 中英文切换后即时更新
- [ ] 所有 button、tooltip、placeholder 中文化

---

## M12 部署差异检查

### [P0] 核心路径

- [ ] 部署后 tag 增删正常（不依赖 localStorage）
- [ ] 隐私模式访问部署地址 → tag 功能正常
- [ ] SERVER_MAPPING 环境变量生效 → 服务器切换正常
- [ ] wss:// 连接建立成功

### [P1] 边界 & 异常

- [ ] 不同电脑/网络访问功能一致
- [ ] HTTPS 无 mixed content 警告
- [ ] 环境变量缺失 fallback 正确（不白屏）

---

## 状态交叉联动测试

> 专门覆盖跨模块的状态交叉场景

### [P0]

- [ ] 搜索 → 切换服务器 → 搜索结果刷新（不显示旧服务器数据）
- [ ] 登录 → 搜索 → 登出 → 标签/树降级而非报错
- [ ] 详情弹窗修改标签 → 关闭 → 过滤器标签列表同步

### [P1]

- [ ] 搜索带过滤器 → 切换服务器 → 过滤器保留/清除行为合理
- [ ] 修改 embedding 配置 → 重新搜索 → 结果变化
- [ ] 快速切换视图模式（grid↔list）无渲染错误
- [ ] 浏览器前进/后退 → URL 参数 & UI 状态同步
- [ ] 多 tab 同时操作 → localStorage 无冲突

---

## 历史翻车定向回归

> 每次发版必测，基于历史踩坑经验

- [ ] [REGR] Tag 功能在隐私模式下可用（不依赖缓存）
- [ ] [REGR] SERVER_MAPPING 非空时服务器别名解析正确
- [ ] [REGR] SelectionModeBar 弹出无 UI 抖动
- [ ] [REGR] Checkbox 点击区域足够大（不难点中）
- [ ] [REGR] 选中后仍可双击查看其他卡片详情
- [ ] [REGR] 动画组件不导致选中延迟
- [ ] [REGR] 过滤器切换不导致选中状态异常
- [ ] [REGR] 关键操作用弹窗而非 toast（Token 过期等）
- [ ] [REGR] 卡片描边无出界
- [ ] [REGR] z-index 图层正确（模态框 > overlay > toolbar > 内容）
- [ ] [REGR] 搜索栏覆盖逻辑不改变卡片位置
- [ ] [REGR] 控制台无红色 Error（重点 wss 连接）
- [ ] [REGR] 拖拽多选逻辑正常（空白处=框选，卡片内=选文本）
- [ ] [REGR] 假数据已替换为真数据（检查 mock 残留）

---

## 通过标准

- **全部 [P0]** 必须通过才能发版
- **[P1]** 允许已知问题存在但必须记录到 KNOWN-PITFALLS.md
- **[REGR]** 必须全部通过（这些都是历史翻车点，不允许回退）
- **控制台无红色 Error** 是硬性要求
