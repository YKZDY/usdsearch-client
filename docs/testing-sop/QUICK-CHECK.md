# 日常快速验收 (QUICK-CHECK)

> **用途**：开发完一个 feat/fix 后快速验证。只覆盖核心路径 [P0]。
> **预计耗时**：~30 分钟
> **使用方法**：确定改动涉及的模块 → 跑该模块 [P0] → 查 MODULE-DEPENDENCY.md 找连带模块 → 跑连带模块 [P0]

---

## M1 搜索核心

- [ ] 输入关键词 → 回车/点击搜索 → 返回结果
- [ ] 搜索空关键词：有默认行为或提示
- [ ] 分页：首页 → 下一页 → 上一页均正常
- [ ] limit 参数生效（默认 50 条，改为 10/100 后结果数正确）
- [ ] URL 参数同步：搜索后 URL 包含 query，刷新页面恢复搜索状态
- [ ] 以图搜图：拖拽图片到搜索框 → 返回相似结果
- [ ] 以图搜图：点击相机图标上传 → 返回结果
- [ ] 清除图片：点击预览 X → 恢复纯文本搜索

---

## M2 搜索过滤器

- [ ] 文件扩展名过滤（include/exclude）生效
- [ ] 路径过滤：输入 search_path → 结果均在该路径下
- [ ] 日期过滤：created_after/before 范围正确
- [ ] 尺寸过滤：file_size_greater_than/less_than 生效
- [ ] 标签过滤：选中标签 → 结果仅含该标签
- [ ] 多个过滤器组合使用时结果正确（AND 逻辑）
- [ ] 清除过滤器 → 恢复全量结果
- [ ] 分类侧边栏选中分类 → 发送 filter_by_tags 到后端
- [ ] 选"所有产品" → 清除分类过滤
- [ ] 二级分类点击生效（如 建筑→地标）

---

## M3 搜索结果展示

- [ ] Grid 视图正常展示缩略图网格
- [ ] List 视图正常展示列表
- [ ] Grid 大小切换（L/S）布局正确
- [ ] "仅显示有预览图的结果" 开关生效
- [ ] 点击结果卡片 → 打开资产详情模态框
- [ ] 结果数量显示（AnimatedCount）与实际一致
- [ ] 卡片 hover 效果正常（阴影/高亮）

---

## M4 图片加载系统

- [ ] 搜索结果缩略图正常加载显示
- [ ] 三级缓存生效：第二次加载同图明显更快
- [ ] 无缩略图的资产显示 placeholder/skeleton

---

## M5 资产详情

- [ ] 模态框打开，正确显示资产名称/路径/属性
- [ ] 缩略图在模态框中正常展示
- [ ] 标签列表正确展示
- [ ] 添加标签 → 立即出现在列表中
- [ ] 删除标签 → 立即从列表中移除
- [ ] 关闭模态框 → 搜索结果中该卡片标签同步更新
- [ ] Reindex 按钮可点击触发

---

## M6 标签系统

- [ ] globalTags 加载完成（TagsFilter 下拉有标签列表）
- [ ] TagsFilter 选中标签 → 搜索结果过滤正确
- [ ] 详情弹窗增删标签后，globalTags 缓存同步更新
- [ ] FabToolbar 中标签筛选正常联动
- [ ] 点击标签候选项 → 标签添加到当前资产（非整页）
- [ ] 部署环境下 tag 增删功能正常

---

## M7 目录树

- [ ] 登录后目录树正常加载展示
- [ ] 点击目录节点 → search_path 填入 → 搜索触发
- [ ] 搜索结果动态注入新路径节点
- [ ] 未登录时显示静态骨架树 fallback

---

## M8 认证 & 服务器

- [ ] Basic Auth 输入用户名密码 → 认证成功 → 可正常搜索
- [ ] 登出：清除凭据 → 回到未认证状态
- [ ] 认证状态持久化：刷新页面后仍保持登录
- [ ] 服务器切换：?server=nucleus URL 参数生效
- [ ] 未认证时搜索仍可用（公共数据）
- [ ] Token 过期时弹出弹窗引导重新登录（非 toast）
- [ ] 未登录状态下搜索 → 弹出登录引导
- [ ] 页面加载时自动检测后端 → 显示 instance info

---

## M9 多选 & 交互

- [ ] 单击卡片 = 查看详情（弹出模态框）
- [ ] Hover 卡片左上角 → 出现圆形 checkbox
- [ ] 点击 checkbox → 选中卡片（不触发详情）
- [ ] 选中 ≥1 张后 → 所有卡片点击变为选中/取消模式
- [ ] 选中状态下双击仍可打开详情
- [ ] 取消所有选中 → 恢复"单击=详情"模式
- [ ] SelectionModeBar 出现时无页面抖动
- [ ] SelectionModeBar 覆盖筛选栏（不造成卡片位移）
- [ ] 多选状态下不允许切换筛选器

---

## M11 i18n & 视觉

- [ ] 中文模式下所有可见文案为中文（无英文残留/key 暴露）
- [ ] toast 提示信息显示中文
- [ ] 弹窗/模态框内文案完整中文化

---

## 浏览器控制台

- [ ] 无红色 Error（整个验收过程中）
